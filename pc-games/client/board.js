class Board {
    constructor(context) {
        this.ctx = context
    }

    // get the cordinates when click upon 
    getClickCordinates(element, ev) {
        const {top, left} = element.getBoundingClientRect()
        const {c}

    }
}

export default Board 